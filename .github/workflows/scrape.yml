name: Daily Fanqie Rank Scraper

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: fanqie-rank-writes-main
  cancel-in-progress: false

on:
  schedule:
    # 每天 UTC 00:00 (北京时间 08:00) 触发
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许手动在界面上点击 Run workflow

jobs:
  scrape-and-commit:
    runs-on: ubuntu-22.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        ref: main
        fetch-depth: 0

    - name: Sync Latest Main
      run: git pull --ff-only origin main

    - name: Check if today's data exists
      id: check_data
      run: |
        TODAY=$(date -u +"%Y%m%d")
        FEMALE="data/fanqie_female_new_ranks_${TODAY}.json"
        MALE="data/fanqie_male_new_ranks_${TODAY}.json"
        if [ -f "$FEMALE" ]; then
          echo "📭 女频今日数据已存在，跳过爬取: $FEMALE"
          echo "need_female=false" >> $GITHUB_OUTPUT
        else
          echo "📭 女频今日数据不存在，将执行爬取"
          echo "need_female=true" >> $GITHUB_OUTPUT
        fi
        if [ -f "$MALE" ]; then
          echo "📭 男频今日数据已存在，跳过爬取: $MALE"
          echo "need_male=false" >> $GITHUB_OUTPUT
        else
          echo "📭 男频今日数据不存在，将执行爬取"
          echo "need_male=true" >> $GITHUB_OUTPUT
        fi

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        cache: 'pip'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        if [ "${{ steps.check_data.outputs.need_female }}" = "true" ] || [ "${{ steps.check_data.outputs.need_male }}" = "true" ]; then
          playwright install --with-deps chromium
        fi

    - name: Run Scraper (女频)
      if: steps.check_data.outputs.need_female == 'true'
      run: python scrape_fanqie_ranks.py --gender female
      env:
        GITHUB_ACTIONS: true

    - name: Run Scraper (男频)
      if: steps.check_data.outputs.need_male == 'true'
      run: python scrape_fanqie_ranks.py --gender male
      env:
        GITHUB_ACTIONS: true

    - name: Build Female Data with AI Summary
      run: python scripts/build_latest.py --gender female
      env:
        API_BASE_URL: ${{ secrets.API_BASE_URL }}
        API_KEY: ${{ secrets.API_KEY }}
        API_MODEL: ${{ secrets.API_MODEL }}

    - name: Build Male Data with AI Summary
      run: python scripts/build_latest.py --gender male
      env:
        API_BASE_URL: ${{ secrets.API_BASE_URL }}
        API_KEY: ${{ secrets.API_KEY }}
        API_MODEL: ${{ secrets.API_MODEL }}

    - name: Sync Latest Main Before Commit
      run: git pull --rebase --autostash origin main

    - name: Commit and Push Changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "[Auto] Update Fanqie daily ranks"
        file_pattern: data api
        branch: main

    # ---- Deploy to GitHub Pages (inline, 绕过 GITHUB_TOKEN 不触发其他 workflow 的限制) ----
    - name: Setup Pages
      uses: actions/configure-pages@v5
      with:
        enablement: true

    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
